---
summary: "Metadata alignment — package.json scripts/fields, Dockerfile LABEL, manifest.json fields, server.json env vars, .env.example restructured, .gitignore/.mcpbignore aligned, docs/tree.md"
breaking: false
security: false
---

# 0.1.4 — 2026-05-23

## Changed

- **`package.json`** `files[]` trimmed (removed `changelog/` and `AGENTS.md`); scripts switched from bare `bun` to `bun run` prefix; `start:stdio`/`start:http` updated to use `bun ./dist/index.js` instead of `node`
- **`Dockerfile`** `LABEL org.opencontainers.image.description` updated to canonical tagline (was "MCP server for Open Library — search 20M+ books…")
- **`manifest.json`** added `repository`, `homepage`, and `license` fields; expanded inline arrays to multi-line JSON
- **`server.json`** HTTP package entry gains `MCP_PUBLIC_URL` env var entry (reverse-proxy override, optional)
- **`.env.example`** restructured to match pubmed gold-standard layout: grouped sections with comments, all current env vars documented
- **`.gitignore`** expanded to full gold-standard allowlist (OS, IDE, Python, Java, Ruby, build, archive, coverage, env, app-specific sections)
- **`.mcpbignore`** adds `.agents/` to excluded paths
- **`docs/tree.md`** regenerated: removes stale `.claude/` root entry, adds `bunfig.toml`
